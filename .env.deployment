# Environment Variables for SkillsMatch.AI - Deployment Template
# Copy this file to .env and fill in your actual API keys

# AI Configuration - REQUIRED
# Get your OpenAI API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Fallback AI - OPTIONAL
# Get your GitHub token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Database Configuration - SQLite Only
# SQLite is optimized for deployment (no external dependencies)
USE_SQLITE=true
STORAGE_TYPE=sqlite

# Application Settings
DATABASE_DEBUG=false

# Optional: AI Model Selection
SKILLMATCH_MODEL=openai/gpt-4o-mini

# Optional: Custom paths (usually not needed)
SKILLMATCH_CONFIG_PATH=config/config.json
SKILLMATCH_SKILLS_DB=data/skills_database.json
SKILLMATCH_OPPORTUNITIES_DB=data/opportunities_database.json

# Deployment Notes:
# 1. For Render.com: Only set OPENAI_API_KEY and USE_SQLITE=true
# 2. SQLite database will be created automatically at web/data/skillsmatch.db
# 3. No separate database service needed
# 4. Data persists with Render's disk storage